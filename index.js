function oddNumbers(inputArray) {
  var accumulator = []

  for (let i = 0; i < inputArray.length; i++) {
    console.log('index is ', i, 'value at that index is', inputArray[i])
  }

  return accumulator
}

oddNumbers([7, 8])
//oddNumbers([1, 2, 3, 4, 5, 6])
